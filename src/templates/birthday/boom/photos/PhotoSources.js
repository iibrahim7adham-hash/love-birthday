// ===========================
// Part 11 — the ONLY place the 3 real photo file paths are defined.
// Drop your 3 image files (JPG/PNG) into public/boom-photos/ using
// these exact filenames — public/ assets are served as-is with no
// import/build wiring, so replacing a file there is all that's needed,
// no code changes. See PhotoPlaceholderGeometry.js's own
// loadPhotoTexture() for how each path becomes a properly-cropped 3D
// texture (cover-crop, no stretching) on the existing card geometry.
// ===========================
export const PHOTO_1 = "/boom-photos/photo-1.png";
export const PHOTO_2 = "/boom-photos/photo-2.png";
export const PHOTO_3 = "/boom-photos/photo-3.png";
