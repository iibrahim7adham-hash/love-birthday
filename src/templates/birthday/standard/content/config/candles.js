const candles = {
  count: 5,
  ringRadius: 0.9, // distance of the candle ring from the cake's center
  height: 0.5,
  stickColor: "#fceec2",

  extinguishStagger: 0.15, // seconds between each candle going out

  flameColor: "#ffb347",
  glowColor: "#ffcf80",
  glowSize: 0.25,

  flickerSpeed: 8,
  flickerStrength: 0.08,

  extinguishDuration: 0.3, // flame shrink-to-nothing duration
  glowFadeDuration: 0.4,
};

export default candles;
