// Who this reel is for. Every scene that needs the celebrant's name or
// age (intro title, the birthday messages in ../messages.js) reads it
// from here — nothing else in the template should hardcode a name or a
// number.
const person = {
  name: "Sara",
  age: 24,
};

export default person;
