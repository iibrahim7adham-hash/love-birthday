// Final closing line, shown beneath the photo heart in Scene 4. Swap this
// per customer once the content pipeline exists — nothing else changes.
export const finalMessage = "Every memory has its place.";
